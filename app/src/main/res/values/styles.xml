<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorThemeOrange">@color/colorThemeOrange</item>
        <item name="colorRed">@color/colorRed</item>
        <item name="colorWhite">@color/colorWhite</item>
    </style>

    <style name="ToolbarTheme" parent="ThemeOverlay.MaterialComponents.ActionBar">
        <item name="android:background">?attr/actionBarItemBackground</item> <!-- Give ripple effect on click -->
        <item name="android:paddingStart">0dp</item>
        <item name="android:paddingEnd">4dp</item>
        <item name="android:paddingTop">1dp</item>
        <item name="android:paddingBottom">4dp</item>
    </style>
</resources>
